# gcp-inventory-agent-vertexai
AI-powered GCP VM inventory agent using Google ADK + Vertex AI - scans VMs,  checks health, pushes to BigQuery, sends Slack reports, and runs automatically  daily via GitHub Actions cron. Auth via Workload Identity Federation - zero stored credentials.
